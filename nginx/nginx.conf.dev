server {
    listen 80;
	server_name localhost;

    location / {
        proxy_pass http://frontend:9000;
    }

    location /api {
        proxy_pass http://backend:8000;
        rewrite ^/api/(.*) /$1 break;
    }
}